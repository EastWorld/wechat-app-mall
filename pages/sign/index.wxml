<van-calendar
  show-title="{{ false }}"
  poppable="{{ false }}"
  min-date="{{ minDate }}"
  max-date="{{ maxDate }}"
  show-confirm="{{ false }}"
  formatter="{{ formatter }}"
/>
<view class="zwqd-box">
  <!-- 高版本系统：显示指纹 emoji -->
  <text wx:if="{{useFingerprintEmoji}}"
    class="emoji-icon"
    bindtap="sign">🫆</text>
  <!-- 低版本或未知系统：继续用原图片 -->
  <image wx:else
    src="/images/zw.png"
    bindtap="sign"></image>
  <view>点击签到</view>
</view>
